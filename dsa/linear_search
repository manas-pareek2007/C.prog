#include<stdio.h>
int ls(int arr[],int n,int x)
{
    for (int i=0;i<n;i++)
    {
        if(arr[i]==x)
        {
            return i;
        }
    
    }
    return -1;
}
int main()
{
int arr[]={5,4,3,2,1};
int n=sizeof(arr)/sizeof(arr[0]);
printf("%d ",ls(arr,n,2));
}
